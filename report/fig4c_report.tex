% Fig. 4C replication report.
% The prose is the author's notebook text, transcribed verbatim. Two kinds of editorial mark:
%   \fillin{} (red)  -- a placeholder in the notebook, filled in
%   \fix{}    (blue) -- a factual correction, always with a footnote giving the original wording
% Every number comes from report/parameters.tex, generated from report/values.json.
\documentclass[11pt,a4paper]{article}

\usepackage[margin=2.5cm]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{ragged2e}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}
\usepackage{enumitem}
\usepackage{caption}

\captionsetup{font=small,labelfont=bf,justification=raggedright,singlelinecheck=false}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.6em}
\RaggedRight                      % left-justified, ragged right edge

\definecolor{fillred}{HTML}{C0392B}
\definecolor{fixblue}{HTML}{1F5FA9}
\definecolor{rulegrey}{HTML}{999999}
\definecolor{boxgrey}{HTML}{F4F4F4}

\newcommand{\fillin}[1]{\textcolor{fillred}{#1}}
\newcommand{\fix}[1]{\textcolor{fixblue}{#1}}

% ---------------------------------------------------------------- code references
% Set \repourl to the repository base (e.g. https://github.com/OWNER/habermas-fig4c) and every
% code reference below becomes a permalink pinned to \repocommitfull. Left empty, they render as
% plain file:line references. This VM cannot create the remote: the exe.dev GitHub proxy serves
% only /repos/OWNER/REPO, so `gh repo create` returns 403.
\newcommand{\repourl}{}
% #1 raw path (for the URL), #2 escaped path (for display), #3 start, #4 end, #5 label
\newcommand{\CodeRef}[5]{%
  \ifx\repourl\empty
    \texttt{\small #2:#3--#4} {\small #5}%
  \else
    \href{\repourl/blob/\repocommitfull/#1\#L#3-L#4}{\texttt{\small #2:#3--#4} {\small #5}}%
  \fi}
\newcommand{\codebox}[1]{%
  \par\vspace{0.2em}%
  \noindent\colorbox{boxgrey}{\parbox{\dimexpr\linewidth-2\fboxsep}{%
    \sloppy\small\textbf{Code:}\; #1}}\par\vspace{0.3em}}

\input{parameters}
\input{codelinks}

\title{Does the Habermas Machine under-represent minority opinions?\\
       \large A replication of Figure 4C of Tessler et al.\ (2024)}
\author{Max Davy}
\date{\today}

\begin{document}
\maketitle

\noindent\colorbox{boxgrey}{\parbox{\dimexpr\linewidth-2\fboxsep}{%
\small\textbf{How to read this document.} The prose is my notebook draft, transcribed unchanged.
\fillin{Red text} fills a placeholder that the draft left open. \fix{Blue text} replaces something
the draft got wrong; a footnote gives the original wording and the source that corrects it. Every
number is generated from \texttt{report/values.json} (see \texttt{report/build\_parameters.py});
none is typed into this document. Code references are pinned to commit \texttt{\repocommit}.}}

\section*{Introduction}

In the original Habermas Machine (HM) paper, the authors consider the possibility that the HM
might exhibit a ``tyranny of the majority'' phenomenon, in which minority perspectives are
under-represented in the HM's output, relative to their prevalence among the population. They
found that it did not do so. To do this, they developed a novel method for describing to what
degree a summary statement reflected majority and minority opinions. This finding is significant
because the HM's design goal was to produce a consensus statement that all participants would
believe represented their opinion. In this report, I aim to replicate this finding using the
publicly-released data from the original HM paper. To cut to the chase, I was unable to replicate
this finding.

\fix{More precisely: the paper's \emph{level} claim does not replicate, and its \emph{direction}
claim does. Under my closest reproduction, minority opinions carry a weight of \oursRevWinLarge{}
in the revised winning statement against the paper's \paperRevWin{} --- below, not above, the
true minority share of \oursShareLarge{} --- and this holds in \sensNCompleted{} of
\sensNCompleted{} specifications I ran. But the rise from the initial winner to the revised winner,
which is the shape of the paper's Figure 4C, does reproduce: $+\diffWinToWinLarge$
($p=\diffWinToWinPLarge$) with ST5-large and $+\diffWinToWinBase$ ($p=\diffWinToWinPBase$) with
ST5-base.}\footnote{Added. The draft stopped at ``I was unable to replicate this finding'', which
is true of the paper's headline but omits that one component of the result does reproduce. Source:
\texttt{results/*/fig4c\_primary.json}, cluster bootstrap over rounds.}

\section*{Approach to reproduction}
\codebox{\codeApproach}

The authors released granular data from the experiments that they ran. They did not release the
code that they used to perform the ``tyranny of the majority'' claim. I used details in their
Supplementary Materials (SM) to attempt to replicate their analysis pipeline. To understand the
deviations between the original analysis and my reproduction, I calculated several estimands
reported in the original paper. There are several points where the SM are ambiguous and leave an
aspect of the analysis pipeline unspecified. I approach this in the following manner: First, I
present the analysis pipeline which I believe is most likely to match the original analysis. Then,
after I have presented this ``most-likely'' pipeline, I perform an exhaustive sensitivity analysis,
in which I vary every ambiguous detail (\fillin{\hyperref[sec:sensitivity]{Sensitivity analysis}}).

\fillin{\textbf{Scope.} This report replicates one claim: the minority-weight result shown as
Figure 4C in the main paper, and expanded as SM Figure S60. The SM's other fairness analyses
(proximity to opinions, human vs.\ model mediator, political affiliation) are out of scope. The
second thing this report does is establish whether the reproduction is trustworthy enough for its
disagreement with the paper to mean anything, which is the subject of the validation checks
reported alongside each step.}

\section*{What is the Habermas Machine (HM)?}

Briefly, the HM is a tool for abstractive summarisation of people's opinions. A group of people
each provide their opinion on a controversial topic. The HM iteratively produces and refines a
consensus statement summarising the opinions in the group. First, it generates $n$ initial
statements. It then ranks these statements according to a simulated vote, in which it attempts to
simulate the preferences of each opinion's author across the initial statements. The first-vote
winner thus produced is shown to each (human) author. Each author provides feedback. This feedback
is provided to the LLM. It generates a second round of candidate statements. These again go through
a simulated vote, resulting in the final statement.

The finding that is the focus of this report is that when one compares the extent to which
minority opinions are represented in the population (proportion of 0.29), this is roughly the same
as how represented minority opinions are in the initial generated statements (0.28), and is
\underline{less} than how well represented minority opinions are in the final statement
(0.36).\footnote{The dotted ``true proportion'' line in SM Figure S60 is at \paperShare{}; the
0.28 and 0.29 bars are the initial candidates and the initial winner respectively. The comparison
in this sentence is therefore between \paperShare{} and \paperRevWin{}.} This is shown in
Figure~\fillin{\ref{fig:paper}}, which is copied from Figure 4(c) in the paper.

\begin{figure}[h]
\centering
\includegraphics[width=0.95\linewidth]{figures/fig_paper.pdf}
\caption{\fillin{The paper's own result, redrawn from the data labels of SM Figure S60
(``Position Embedding'' panel), which SM 5.2.2 identifies as the expanded version of main-text
Figure 4C. Dotted line: the true proportion of minority opinions (\paperShare). Error bar on the
revised winner: $\pm\paperRevWinSE$, as printed in the paper. $n=\paperNRounds$ rounds.}}
\label{fig:paper}
\end{figure}

\section*{What is a minority (or majority) group?}
\codebox{\codeMinority}

In the papers' experiments, groups of between \fillin{\groupMin--\groupMax} people took part in
the deliberative process mediated by the HM.\footnote{\fillin{The study design assigned
participants to groups of \paperGroupSize{} (SM 1.2); in the \groupNRounds{} pre-registered rounds
of cohorts 1--3 that this analysis uses, \groupModalShare{} of rounds have \groupModal{} opinions
and the remainder have \groupMin{}, after the paper's own exclusions.}} Before writing their
free-text opinion, they indicated their level of agreement with a political proposition using a
Likert scale of 1--7. It was thus possible to provide a neutral (4) opinion. A group was defined as
having a minority group if there was an unequal amount of agreeing (5--7) and disagreeing (1--3)
opinions. Groups without a minority group were excluded from this analysis.

\fillin{\textbf{Reproduction parameters.} Minority membership follows SM 5.4.1 literally: the
minority is whichever side of neutral holds fewer opinions, and everything else --- majority
opinions and neutral (4) ratings alike --- is non-minority.\footnote{SM 5.4.1: ``we may define the
`minority' side of the Likert scale as the side that contains the smaller number of opinions, and
define all other opinions as `non-minority' opinions---which include `majority' opinions [\ldots]
as well as `neutral' opinions (i.e.\ with a Likert rating of 4), if any.''} Rounds with equal
numbers on each side are excluded. The primary sample is the pre-registered groups of cohorts
1--3: \groupNRounds{} rounds, of which \oursNRoundsLarge{} contain a minority under this rule,
giving a true minority share of \oursShareLarge{}. The paper reports \paperShare{}; the gap is
almost entirely the treatment of neutral raters, and both alternatives are carried through the
\hyperref[sec:sensitivity]{sensitivity analysis} below.}

\section*{How does the HM paper measure how well-represented minority opinions are?}

This part of the analysis has two components. First, each participant's free-text opinion and the
statements produced by the HM (including intermediate statements produced in its iterative process)
are given a scalar score to measure to what extent they agree or disagree with the political
proposition. I refer to this score as the ``agreement score''.

The agreement scores of original human opinions and LLM-generated statements are then used to
measure how similar the average agreement scores among a minority group are, to the agreement score
of the LLM-generated statement.

I now detail my reproduction of each of these steps.

\section*{Measuring agreement score}
\codebox{\codeAgreement}

To measure the agreement score of a human-authored opinion or a LLM-generated statement, it is
first mapped into a 768-dimension embedding, and then projected onto the vector going between two
points.

\fix{There is one main detail missing from the SM. The exact phrases used to construct the
endpoints of this projection vector \emph{are} given}\footnote{The draft read: ``There are two main
details missing from the SM. First, we don't have the exact phrases used to construct the endpoints
of this projection vector.'' SM 5.1.2 specifies them: the endpoints are a ``generic +
question-specific combination'', i.e.\ the generic prefix followed by the question's affirming or
negating position statement. Only the model size remains genuinely unspecified, so the count of
missing details drops from two to one.} \fillin{--- SM 5.1.2 fixes them as the generic prefix
concatenated with the question's own position statement, i.e.\ ``Yes, I agree. It is the
government's role to [\ldots]'' and ``No, I disagree. It is not the government's role to
[\ldots]'', with the axis taken as the unit vector from the negating to the affirming endpoint (SM
eq.~3) and the score as the projection onto it (SM eq.~5). This is the ``prefixed'' endpoint style
in the code above.} \fix{What we do not know} is what exact embedding model was used for this
process. While the SM describes Sentence-T5, there are various sizes of this model that could be
used.

\subsection*{Validating the agreement score}
\codebox{\codeAgreementValidation}

The original authors validate their agreement score method by using the original human responses,
comparing the human's self-reported Likert scores against the estimated agreement scores derived
from the method above. In the original paper, they report an $R^2$ of
\paperMarginalRsq{}.\footnote{Correct as written, and worth pinning down: this is the
\emph{marginal} $r^2$ of the random-effects model of SM eq.~7 (Likert rating on position score,
random intercept per round). The paper's conditional $r^2$ is \paperConditionalRsq{}. My Table~1
therefore reports the same marginal quantity, not a Pearson $r^2$.} In our reproduction, the $R^2$
of each embedding model is shown in Table~\fillin{\ref{tab:rsq}}.

\begin{table}[h]
\centering
\small
\caption{\fillin{Marginal $r^2$ of the paper's validation model (SM eq.~7), by embedding model.
Fitted on the \oursMixedNLarge{} opinions of the \oursMixedNRoundsLarge{} pre-registered rounds of
cohorts 1--3, random intercept per round. Pearson $r$ is the main-text Figure 4A quantity, shown
for comparison.}}
\label{tab:rsq}
\begin{tabular}{lccc}
\toprule
 & marginal $r^2$ & conditional $r^2$ & Pearson $r$ \\
\midrule
\fillin{sentence-t5-base}   & \fillin{\oursMarginalRsqBase}  & \fillin{\oursConditionalRsqBase}  & \fillin{\oursPearsonRBase} \\
\fillin{sentence-t5-large}  & \fillin{\oursMarginalRsqLarge} & \fillin{\oursConditionalRsqLarge} & \fillin{\oursPearsonRLarge} \\
\fillin{sentence-t5-xlarge} & \multicolumn{3}{c}{\fillin{not evaluated --- see note}} \\
\midrule
\fillin{paper}              & \fillin{\paperMarginalRsq} & \fillin{\paperConditionalRsq} & \fillin{\paperFigAr} \\
\bottomrule
\end{tabular}
\end{table}

\fillin{The xlarge row is empty for a hardware reason rather than a scientific one: sentence-t5-xl
is a 3B-parameter model and this machine has no GPU and 7~GB of RAM, so it could not be embedded
here. The two sizes that could be run bracket the question well enough to answer it --- base falls
well short of the paper's \paperMarginalRsq{} and large lands essentially on it --- and the
direction of the remaining risk is worth stating plainly: if the authors used a larger ST5 than
large, the axis would be slightly \emph{better} than mine, and a better axis moves the minority
weight further from the paper's value, not closer (\hyperref[sec:sensitivity]{Sensitivity analysis}).}

Based on these results, we choose model \fillin{sentence-t5-large} as the most-likely model used in
the original analysis. \fillin{Its marginal $r^2$ of \oursMarginalRsqLarge{} is within
\oursMarginalRsqGapLarge{} of the paper's \paperMarginalRsq{}, and its Pearson $r$ of
\oursPearsonRLarge{} against the paper's \paperFigAr{} says the same thing: the position axis this
report is built on is about as good as the one the paper used. Results for sentence-t5-base are
reported throughout as a robustness check.}

\section*{Measuring representativeness}
\codebox{\codeRepresentation}

Given a set of human-authored opinions $\{x_i \mid i \in 1,\dots,n\}$, a partition of opinions into
minority $M$ \& non-minority $N$ sets, agreement scores $\{a_i \mid i \in 1,\dots,n\}$, the
``representation'' of an opinion $x_i$ is given by $\lambda_i$ in
%
\begin{equation*}
\text{set } \lambda_i \text{ to minimise } \Big(a_0 - \sum_i \lambda_i a_i\Big)^2
\qquad\text{such that}\qquad \sum_i \lambda_i = 1 \ \ \&\ \ \lambda_i \geq 0 .
\end{equation*}
%
To calculate the representation of $M$ \& $N$, simply calculate the average representation of their
elements: $\lambda_M := \frac{1}{|M|}\sum_{x_i \in M} \lambda_i$.

\subsection*{The implementation check}
\codebox{\codeSanity}

To test that the implementation of this procedure works as expected, the original authors ran a
check where \fix{each human opinion in turn is treated as the target and regressed on convex
combinations of that same set of opinions. Averaged over the set, the recovered minority weight
should come back as simply the proportion of opinions in the
minority.}\footnote{Corrected. The draft read: ``the output statement was simply one of the $m$
human opinion inputs, and repeated this $m$ times (once for each input). [this may be incorrect]
After averaging across these runs, the average representation $\bar\lambda_i$ of an input $x_i$
should be $\frac{1}{m}$.'' The draft's own flag was right. The paper's Materials and Methods
describe it as: ``As a sanity check, we similarly regress individual opinions on convex
combinations of those same opinions, which we verify to simply yield the proportion of opinions in
the minority.'' The target of the check is the \emph{minority share}, not $1/m$ per input.} We
replicate this implementation check. \fillin{It passes: the recovered weight is
\oursOpinionsLarge{} against a true share of \oursShareLarge{} (identical to three decimal places
under both embedding models, since the check does not depend on the statements). This is the
leftmost bar of Figure~\ref{fig:contrast}.}

\section*{Results}
\phantomsection\label{sec:results}
\codebox{\codeResults}

We fail to replicate the results from the original paper. Figure~\fillin{\ref{fig:contrast}}
displays an adjusted version of Figure~\fillin{\ref{fig:paper}} above, where we contrast the
original paper's results against our own.

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{figures/fig_contrast.pdf}
\caption{\fillin{Minority weight at each phase: the paper against this reproduction, under both
embedding models. Error bars are the analytic standard error of the constrained fit. The two dotted
lines are the true minority share in each case (\paperShare{} for the paper, \oursShareLarge{} here;
the difference is the treatment of neutral raters). $n=\oursNRoundsLarge$ rounds with a minority,
from \oursNRoundsTotalLarge{} pre-registered rounds in cohorts 1--3.}}
\label{fig:contrast}
\end{figure}

\fillin{Three things in that figure are worth separating. The sanity check reproduces exactly ---
the leftmost bars sit on their respective true shares --- so the regression machinery is doing what
the paper says it does. Every statement phase, by contrast, sits below the true share rather than
at or above it: the revised winner carries \oursRevWinLarge{} $\pm$ \oursRevWinSELarge{} under
ST5-large and \oursRevWinBase{} $\pm$ \oursRevWinSEBase{} under ST5-base, against the paper's
\paperRevWin{}. And the shape differs: in the paper the four phases rise monotonically, whereas
here the winner-selection step \emph{drops} minority weight at both rounds (initial candidates
\oursInitCandLarge{} $\rightarrow$ initial winner \oursInitWinLarge{}), and the revision step
partially restores it (\oursRevWinLarge{}). The paper's rise from initial winner to revised winner
is therefore present ($+\diffWinToWinLarge$, 95\% CI $[\diffWinToWinCIloLarge,
\diffWinToWinCIhiLarge]$, $p=\diffWinToWinPLarge$), but it starts from a lower base and does not
carry the final statement above proportionality; measured from the initial candidates instead, the
change over the whole process is $\diffInitToFinalLarge$ (95\% CI $[\diffInitToFinalCIloLarge,
\diffInitToFinalCIhiLarge]$).}

\subsection*{Is the reproduction good enough to disagree with the paper?}
\codebox{\codeValidationExtra}

\fillin{Everything above is only interesting if the pipeline is sound, so the checks that bear on
that are collected in Table~\ref{tab:validation}. Three of the four independent checks land on the
paper's values, which is the basis for reporting the disagreement rather than assuming a bug. The
one that does not --- the share of statements falling inside the range of their group's opinions
--- is lower here than in the paper, and is the clearest remaining candidate for an unmodelled
difference.}

\begin{table}[h]
\centering
\small
\caption{\fillin{Checks on the reproduction, independent of the minority-weight result.}}
\label{tab:validation}
\begin{tabular}{p{7.4cm}cc}
\toprule
\fillin{check} & \fillin{paper} & \fillin{ours (ST5-large)} \\
\midrule
\fillin{Sanity check: opinions regressed on themselves recover the minority share} & \fillin{\paperOpinions{} vs \paperShare{}} & \fillin{\oursOpinionsLarge{} vs \oursShareLarge{}} \\
\fillin{Position axis quality: marginal $r^2$ of Likert on position score} & \fillin{\paperMarginalRsq} & \fillin{\oursMarginalRsqLarge} \\
\fillin{Fig.\ 4A: correlation of position score with Likert rating} & \fillin{\paperFigAr} & \fillin{\oursFigArLarge} \\
\fillin{Fig.\ 4B: statements falling within the range of their group's opinions} & \fillin{\paperFigBwithin} & \fillin{\oursFigBwithinLarge} \\
\midrule
\fillin{Regression on the full 768-d embedding rather than the scalar score (initial / revised)} & \fillin{---} & \fillin{\oursFullDimInitLarge{} / \oursFullDimRevLarge} \\
\fillin{Axis-noise simulation: weight recovered when the axis is degraded to $r=\oursAttenMinRLarge$} & \fillin{---} & \fillin{\oursAttenRecoveredLarge{} vs \oursShareLarge} \\
\fillin{Winning statement lies inside the group's opinion range (initial / revised)} & \fillin{---} & \fillin{\oursWinnerInsideInitLarge{} / \oursWinnerInsideRevLarge} \\
\bottomrule
\end{tabular}
\end{table}

\fillin{The last three rows are the ones that rule out the obvious mechanical explanations. If the
scalar projection were discarding the information that carries minority weight, regressing on the
full 768-dimensional embedding should recover the paper's value; it gives \oursFullDimInitLarge{}
and \oursFullDimRevLarge{}, still below the true share. If a noisy position axis were attenuating
the estimate towards zero, then degrading the axis deliberately should bias the recovered weight
downwards; it does not --- at $r=\oursAttenMinRLarge$ the simulation still recovers
\oursAttenRecoveredLarge{} against a true \oursShareLarge{}. A downward-biased axis is therefore
not what produces the gap.}

\section*{Sensitivity analysis}
\phantomsection\label{sec:sensitivity}
\codebox{\codeSensitivity}

As noted above, there are several aspects of the analysis plan which are not specified in the SM.
These are:

\begin{table}[h]
\centering
\small
\caption{\fillin{Underspecified choices, the options considered, and the option taken in the
primary specification above.}}
\label{tab:ambiguity}
\begin{tabular}{p{4.1cm}p{6.2cm}p{4.4cm}}
\toprule
\fillin{ambiguity} & \fillin{possible options} & \fillin{option chosen above} \\
\midrule
\fillin{ST5 model size} & \fillin{base, large, xlarge, xxl} & \fillin{large (Table~\ref{tab:rsq})} \\
\fillin{Neutral (Likert 4) raters} & \fillin{count as non-minority; drop the rater; drop the whole group} & \fillin{count as non-minority (SM 5.4.1)} \\
\fillin{Groups with equal numbers either side} & \fillin{exclude; keep with the agreeing side as minority; keep with the disagreeing side as minority} & \fillin{exclude} \\
\fillin{Basis for splitting minority / non-minority} & \fillin{Likert rating; sign of the position score (SM Fig.\ S62)} & \fillin{Likert rating (SM Fig.\ S60)} \\
\fillin{Column order of the design matrix} & \fillin{data order; sorted by score; random} & \fillin{data order} \\
\fillin{Sample} & \fillin{cohorts 1--3 pre-registered; all cohort 1--3 rounds; each cohort alone; cohort 4} & \fillin{cohorts 1--3, pre-registered} \\
\bottomrule
\end{tabular}
\end{table}

I reran the analysis with every combination of these decisions
(\fillin{\sensNAttempted{}} runs).\footnote{\fillin{\sensNAttempted{} specifications were run per
embedding model, of which \sensNCompleted{} completed: \sensNAnalytic{} vary an analytic choice on
the primary sample and \sensNSample{} swap the sample. The \sensNFailed{} that did not complete are
the training-data and virtual-citizens'-assembly samples, whose texts fall outside the
pre-registered set that was embedded; both are outside the scope of main-text Figure 4C in any
case.}} Across the runs, I calculated the following metrics:

\begin{itemize}[leftmargin=1.4em,itemsep=0.25em]
  \item Proportion of runs where minority representation increases from initial to final:
        \fillin{\sensPropInitToFinalLarge{} measured from the initial candidates to the revised
        winner, and \sensPropWinToWinLarge{} measured winner-to-winner, which is the contrast
        Figure 4C draws. (ST5-base: \sensPropInitToFinalBase{} and \sensPropWinToWinBase{}.)}
  \item Proportion of runs where minority representation is larger in final than the prevalence
        among population (\fix{\oursShareLarge}):\footnote{Corrected. The draft gave 0.291. That is
        the true share under one variant --- neutral raters dropped --- not under the primary
        specification, where it is \oursShareLarge{}. Each run is compared against its own true
        share rather than a single fixed number, since the minority rule changes the share as well
        as the weight.}
        \fillin{\sensPropAboveShareLarge{} (ST5-base: \sensPropAboveShareBase{}). The largest
        revised-winner weight found anywhere in the sweep is \sensMaxRevWinLarge{}, against a mean
        true share of \sensTrueShareMeanLarge{}.}
  \item Average representation at each of the 4 stages, with 5th \& 95th percentiles:
        \fillin{shown in Figure~\ref{fig:sensitivity}.}
\end{itemize}

\begin{figure}[t]
\centering
\includegraphics[width=0.92\linewidth]{figures/fig_sensitivity.pdf}
\caption{\fillin{Mean minority weight at each phase across the \sensNCompleted{} completed
specifications, with 5th--95th percentile whiskers, for both embedding models. Dotted line: the
mean true minority share across specifications (\sensTrueShareMeanLarge{}). No specification places
the revised winner above its own true share.}}
\label{fig:sensitivity}
\end{figure}

\section*{Discussion}

If the above reproduction of the analysis pipeline is correct, then it would appear that the HM
actually suppresses minority opinions, if we follow the original paper's interpretation of the
above measurement method.

Notably, we are able to reproduce several parameter estimates from the paper, including the
validation of the agreement-score embedding-based approach and the implementation check of the
regression method. We fail to replicate the substantive findings for this part of the paper,
however.

This finding actually aligns somewhat with the finding that people's beliefs shifted towards the
majority position (although there are many possible explanations for this finding).

\fillin{Two caveats belong next to that conclusion. The first is that ``suppresses'' is a statement
about the measurement, not about the mechanism: every phase of the process, including the initial
candidates that no vote has touched yet, sits below proportionality, which is as consistent with
the position axis compressing minority-side statements as with the HM discounting minority
opinions. The second is that the one part of the paper's pattern that does reproduce --- revision
raising minority weight --- is evidence the HM's feedback step does what the paper claims for it,
even though it starts from and ends at a lower level here.}

\subsection*{Open notes}

\fillin{Carried over from the draft, unresolved:} need to read paper $\rightarrow$ raises an
interesting question as to why and whether minority group members felt their opinion was captured
in final.

\fillin{One word of that note was not legible in the source; the sense is preserved. The
end-of-round survey data needed to answer it
(\texttt{hm\_all\_round\_survey\_responses.parquet}) is already downloaded, but the question is a
different one from Figure 4C and is out of scope here.}

\end{document}
